# Committed baseline for `run --cache-only` (CI cache-completeness invariant):
# the maximum allowed keep-Swedish fallbacks in a backend-free full mirror regen.
# Measured live 2026-07-22: 9 = the stale code-cache give-up entries the loader
# deliberately drops on every load to retry against a model; with no backend each
# becomes one keep-Swedish fallback. The regen was byte-identical to the committed
# mirror (git clean), so these 9 are a stable floor, not drift — see SM200.
# Lower only, never raise without a reviewed reason.
9
