# Committed baseline for `run --cache-only` (CI cache-completeness invariant):
# the maximum allowed keep-Swedish fallbacks in a backend-free full mirror regen.
# Current value = the one known uncached unit (a code-prose give-up that the
# code-cache loader deliberately drops each run to retry) — see SM200.
# Re-verify with a live --cache-only run before wiring CI; lower only, never raise
# without a reviewed reason.
1
