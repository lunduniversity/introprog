package maze

import graphics.SimpleTurtle

class MazeWalker(val turtle: SimpleTurtle) {
  private def delay(millis: Int) = Thread.sleep(millis)

  def walk(maze: Maze, animationDelay: Int): Unit = {
    println("*** Saknad implementation: gå i labyrint")
  }
}
